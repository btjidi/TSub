{$TSUB_DOMAIN} {
    encode zstd gzip
    reverse_proxy {$TSUB_UPSTREAM:127.0.0.1:8787}
    header {
        -Server
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
        X-Content-Type-Options "nosniff"
        Referrer-Policy "no-referrer"
    }
}
